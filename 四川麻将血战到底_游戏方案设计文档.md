# 指尖麻将 — 四川麻将血战到底 游戏方案设计文档

---

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | 指尖麻将-四川麻将血战到底 游戏方案设计文档 |
| 版本号 | V1.1（进度纠偏版） |
| 创建日期 | 2026-07-15 |
| 文档类型 | 方案设计文档 / 当前进度纠偏说明 |

---

## 一、项目概述

### 1.1 项目背景

四川麻将"血战到底"是国内最受欢迎的麻将玩法之一，以其独特的"缺一门"机制、"血战到底"（一局多次胡牌）、"刮风下雨"（杠牌加番）等特色规则著称。本项目旨在开发一款玩法规则与"指尖麻将-四川麻将血战到底"完全一致的手机麻将游戏，提供流畅的对战体验与完整的社交功能。

### 1.2 项目目标

- 完整还原四川麻将血战到底的全部规则与番型体系
- 提供真人匹配对战、好友约战、AI对战等多种模式
- 支持完整的用户账户系统与数据统计
- 适配主流手机屏幕，横屏操作体验流畅

### 1.3 技术定位

采用 **Web前端 + 后端服务** 架构，前端使用 HTML5 Canvas 渲染，可运行于浏览器或通过 WebView 封装为 Android/iOS App。**不使用 Unity、Cocos Creator 等需要手动编译构建的重型引擎**，开发与部署流程简洁高效。

### 1.4 当前项目真实状态（2026-07-27更新）

当前仓库已经完成**可运行的单机麻将核心、电脑玩家、联网服务、账号服务和移动端打包工程**，但仍未完成 Android/iOS 真机验收，因此暂不能称为已上线产品。

通俗理解：麻将桌、发牌员、裁判、电脑牌友和联网房间已经能工作；现在剩下的是把安装包放到真实手机上，确认横屏、触屏和长时间运行都稳定。

| 项目内容 | 当前状态 | 说明 |
|----------|----------|------|
| 前端页面原型 | 已完成原型 | 已有 `index.html`、`style.css`、`app.js`，可展示牌桌、手牌、按钮、结算面板 |
| 牌面与视觉风格 | 已完成原型 | 已实现科幻风麻将牌 SVG 绘制、头像、动效、展示柜 |
| 定缺交互 | 已完成 | 四家定缺、缺门优先出牌、缺门胡牌限制均已接入 |
| 出牌/摸牌 | 已完成 | 使用真实 108 张牌墙，记录牌河并实时更新剩余牌数 |
| 碰/杠/胡按钮 | 已完成 | 按合法性和操作优先级显示，并真实更新牌局状态 |
| 结算页面 | 已完成 | 绑定番型、封顶、杠分、退税和最终积分 |
| 麻将规则引擎 | 已完成 | 覆盖胡牌、七对、龙七对、清一色、对对胡、查叫、查花猪等规则 |
| 四人单机对战 | 已完成 | 支持轮流摸打、血战继续、流局和终局判断 |
| AI电脑玩家 | 已完成 | 支持初级、中级、高级策略以及碰杠胡和操作延迟 |
| 联网对战 | 已完成 | 已有 WebSocket、房间、准备、开始、离开、重连和服务端校验 |
| 用户账户系统 | 已完成 | 已有注册、登录、游客、金币、战绩、排行榜和数据库表 |
| 移动端打包 | 部分完成 | Capacitor、Android/iOS 工程和 Android 调试包已完成，真机尚未验收 |
| 自动化测试 | 已完成 | 全量 108 项通过，另有 100 盘随机机器人对局通过 |

因此，本方案后续开发应从“UI 原型”推进到“单机可玩版本”，再进入联网和上线阶段。

---

## 二、系统架构设计

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                     客户端层 (Client)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │  Vue 3   │  │  Canvas  │  │ WebSocket│  │  Audio   │ │
│  │  UI框架  │  │ 游戏渲染  │  │ 实时通信  │  │  音效    │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
├─────────────────────────────────────────────────────────┤
│                     网关层 (Gateway)                      │
│  ┌──────────────────────────────────────────────────────┐│
│  │         Nginx 反向代理 + WebSocket 代理               ││
│  └──────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────┤
│                     服务层 (Service)                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ 房间服务  │ │ 对战服务  │ │ 用户服务  │ │ AI服务   │  │
│  │Room Svc  │ │Game Svc  │ │User Svc  │ │AI Svc    │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                │
│  │ 匹配服务  │ │ 结算服务  │ │ 数据统计  │                │
│  │Match Svc │ │Settle Svc│ │Stats Svc │                │
│  └──────────┘ └──────────┘ └──────────┘                │
├─────────────────────────────────────────────────────────┤
│                     数据层 (Data)                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────────┐  │
│  │  MySQL   │  │  Redis   │  │  对象存储 OSS        │  │
│  │  持久化   │  │  缓存/锁  │  │  头像/资源文件       │  │
│  └──────────┘  └──────────┘  └──────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 层级 | 技术方案 | 说明 |
|------|----------|------|
| 前端框架 | Vue 3 + TypeScript + Vite | 组件化开发，TypeScript 类型安全 |
| 状态管理 | Pinia | Vue 3 官方推荐状态管理 |
| UI组件库 | 自研麻将专用组件 | 高度定制化的麻将UI组件 |
| 游戏渲染 | HTML5 Canvas 2D | 轻量高效，无需WebGL |
| 实时通信 | WebSocket (Socket.IO) | 双向实时消息推送 |
| 后端框架 | Node.js + TypeScript + Express | 全栈TypeScript，前后端类型共享 |
| ORM | Prisma | 类型安全的数据库操作 |
| 数据库 | MySQL 8.0 | 持久化存储 |
| 缓存/锁 | Redis | 房间状态缓存、分布式锁、匹配队列 |
| AI引擎 | 纯TypeScript决策树 | 内嵌于服务端，无需额外部署 |
| 移动端封装 | Capacitor / WebView | 一键打包APK/IPA |
| 部署 | Docker + Nginx | 容器化部署 |

### 2.3 项目目录结构

```
mahjong-game/
├── client/                      # 前端项目
│   ├── src/
│   │   ├── components/          # UI组件
│   │   │   ├── game/            # 游戏核心组件(牌桌、手牌、操作区)
│   │   │   ├── room/            # 房间相关组件
│   │   │   ├── lobby/           # 大厅组件
│   │   │   └── common/          # 通用组件
│   │   ├── engine/              # 麻将引擎(前端验证用)
│   │   │   ├── tiles.ts         # 牌定义
│   │   │   ├── rules.ts         # 规则引擎
│   │   │   └── ai.ts            # 前端AI(离线模式)
│   │   ├── stores/              # Pinia状态管理
│   │   ├── views/               # 页面视图
│   │   ├── audio/               # 音效资源
│   │   └── assets/              # 图片/字体资源
│   └── index.html
├── server/                      # 后端项目
│   ├── src/
│   │   ├── services/            # 业务服务
│   │   │   ├── game/            # 游戏逻辑服务
│   │   │   ├── room/            # 房间服务
│   │   │   ├── match/           # 匹配服务
│   │   │   ├── ai/              # AI服务
│   │   │   ├── user/            # 用户服务
│   │   │   └── settle/          # 结算服务
│   │   ├── engine/              # 麻将核心引擎
│   │   │   ├── core.ts          # 核心状态机
│   │   │   ├── rules.ts         # 规则判定
│   │   │   ├── fan.ts           # 番型计算
│   │   │   ├── settle.ts        # 结算逻辑
│   │   │   └── ai-strategy.ts   # AI策略
│   │   ├── models/              # 数据模型
│   │   ├── routes/              # API路由
│   │   └── websocket/           # WebSocket处理
│   └── prisma/
│       └── schema.prisma        # 数据库Schema
└── shared/                      # 前后端共享类型定义
    └── types/
        ├── game.ts              # 游戏核心类型
        ├── tile.ts              # 牌相关类型
        └── protocol.ts          # 通信协议类型
```

---

## 三、模块划分与功能说明

### 3.1 模块总览

| 编号 | 模块名称 | 职责描述 |
|------|----------|----------|
| M1 | 游戏规则引擎 | 番型判定、胡牌检测、结算计算 |
| M2 | 游戏流程控制 | 发牌、摸牌、出牌、碰杠胡的状态机 |
| M3 | 房间与对局系统 | 创建/加入房间、匹配、观战 |
| M4 | AI对手系统 | 多难度AI出牌策略 |
| M5 | 用户与账户系统 | 注册登录、金币积分、战绩 |
| M6 | UI/UX交互系统 | 界面布局、牌桌渲染、操作交互 |

### 3.2 M1 — 游戏规则引擎

**功能说明：**

游戏规则引擎是整个系统的核心，负责所有规则的判定与计算。

**子模块：**

| 子模块 | 功能 |
|--------|------|
| 牌库管理 | 108张标准麻将牌(万/条/筒各36张)的生成、洗牌、发牌 |
| 胡牌检测 | 判断手牌是否满足胡牌条件(基本胡+番型要求) |
| 番型计算 | 遍历所有番型，计算总番数 |
| 缺一门检测 | 判断玩家是否已缺一门花色 |
| 听牌检测 | 判断当前手牌是否已听牌(下叫) |
| 杠牌判定 | 明杠/暗杠/补杠的合法性判定 |

### 3.3 M2 — 游戏流程控制

**功能说明：**

管理一局游戏从开始到结束的完整生命周期。

**状态机设计：**

```
等待开始 → 发牌 → 定缺 → 正常出牌 ←→ 碰/杠操作
                                    ↓
                                胡牌判定
                                    ↓
                        血战继续 / 流局结算
                                    ↓
                                终局结算
```

**子模块：**

| 子模块 | 功能 |
|--------|------|
| 回合管理 | 轮流操作、超时处理、自动摸打 |
| 操作判定 | 碰、杠(明杠/暗杠/补杠)、胡的合法性 |
| 优先级仲裁 | 多人同时操作时的优先级(胡>杠>碰>吃-四川麻将无吃) |
| 血战控制 | 已胡牌玩家跳过回合，未胡牌玩家继续 |
| 流局处理 | 查大叫、查花猪逻辑 |

### 3.4 M3 — 房间与对局系统

**功能说明：**

提供玩家之间的对局组织与管理。

**子模块：**

| 子模块 | 功能 |
|--------|------|
| 房间创建 | 创建房间，设置局数(4/8局)、底分、封顶倍数 |
| 快速匹配 | 按段位/积分自动匹配对手 |
| 好友约战 | 通过房间号/邀请码进入 |
| 观战系统 | 实时观看他人对局 |
| 断线重连 | 掉线后自动恢复对局状态 |

### 3.5 M4 — AI对手系统

**功能说明：**

提供不同难度的电脑玩家，用于单机练习或补位。

**子模块：**

| 子模块 | 功能 |
|--------|------|
| 初级AI | 随机出牌，仅做基本防守 |
| 中级AI | 优先打缺门、基础攻防 |
| 高级AI | 听牌判断、防守拆牌、杠牌策略 |

**AI策略要点：**
- 缺一门优先：优先打出需要缺的花色
- 搭子价值评估：对子 > 两面搭子 > 边张搭子 > 单张
- 防守逻辑：推测对手听牌范围，避免放炮
- 杠牌判断：评估杠牌收益与风险

### 3.6 M5 — 用户与账户系统

**功能说明：**

管理玩家账户、资产与游戏数据。

**子模块：**

| 子模块 | 功能 |
|--------|------|
| 注册登录 | 手机号/微信快捷登录 |
| 个人中心 | 昵称、头像、段位管理 |
| 金币体系 | 金币获取、消耗、对局奖惩 |
| 积分体系 | ELO/天梯积分计算 |
| 战绩统计 | 对局记录、胡牌率、胜率等 |
| 排行榜 | 金币榜、积分榜、胜率榜 |

### 3.7 M6 — UI/UX交互系统

**功能说明：**

所有用户界面的设计与交互逻辑。当前仓库已经实现了一套**全息赛博朋克科幻风格前端交互原型（Sci-Fi Holographic Console UI）**。

注意：这里的“原型”指的是界面能展示、按钮能点击、动画能播放；不等于完整游戏逻辑已经完成。完整游戏还需要规则引擎、对局流程、电脑玩家、后端服务共同支撑。

*   **HTML5 页面布局**：[index.html](file:///d:/桌面/游戏设计/发财/index.html)
*   **全息霓虹样式系统**：[style.css](file:///d:/桌面/游戏设计/发财/style.css)
*   **交互与渲染引擎逻辑**：[app.js](file:///d:/桌面/游戏设计/发财/app.js)
*   **编译后生产环境入口**：[dist/index.html](file:///d:/桌面/游戏设计/发财/dist/index.html) (支持一键部署)

**界面清单：**

| 页面 | 功能描述 | 实现状态 |
|------|----------|----------|
| 登录/注册页 | 手机号登录、微信授权 | 概念预留 |
| 游戏大厅 | 模式选择(匹配/创建房间/好友约战/AI对战) | 概念预留 |
| 游戏桌面 | 核心游戏界面(手牌、牌河、操作按钮) | **已实现前端交互原型** |
| 结算页面 | 本局番型明细、得分统计、继续/退出 | **已实现结算界面原型，未接真实结算规则** |
| 个人中心 | 账户信息、战绩、设置 | 概念预留 |
| 房间界面 | 房间信息、玩家列表、准备/开始 | 概念预留 |

---

## 四、核心数据结构定义

### 4.1 麻将牌定义

```typescript
// shared/types/tile.ts

/** 花色枚举 */
enum TileSuit {
  WAN   = 'wan',    // 万 (1-9万)
  TIAO  = 'tiao',   // 条 (1-9条)
  TONG  = 'tong',   // 筒 (1-9筒)
}

/** 单张牌 */
interface Tile {
  id: number;        // 唯一ID (0-107)
  suit: TileSuit;    // 花色
  value: number;     // 点数 (1-9)
  code: string;      // 编码，如 "W1", "T5", "T9"
}

/** 牌墙 (108张) */
type TileWall = Tile[];

/** 手牌 (含已碰/杠副露) */
interface HandTiles {
  concealed: Tile[];              // 手牌(暗牌)，已排序
  melds: Meld[];                  // 副露(碰/杠)
  flowerTiles: Tile[];            // 花牌(四川麻将无花牌，预留)
}

/** 副露(碰/杠组) */
interface Meld {
  type: 'pong' | 'ming_kong' | 'an_kong' | 'bu_kong';
  tiles: Tile[];                  // 3或4张牌
  fromPlayer?: number;            // 来源玩家索引(暗杠无)
}
```

### 4.2 游戏状态定义

```typescript
// shared/types/game.ts

/** 游戏阶段 */
enum GamePhase {
  WAITING    = 'waiting',      // 等待玩家
  DEALING    = 'dealing',      // 发牌中
  DING_QUE   = 'ding_que',     // 定缺中(选择缺哪门)
  PLAYING    = 'playing',      // 正常出牌
  SETTLING   = 'settling',     // 结算中
  FINISHED   = 'finished',     // 已结束
}

/** 玩家操作 */
enum ActionType {
  DRAW       = 'draw',         // 摸牌
  DISCARD    = 'discard',      // 出牌
  PONG       = 'pong',         // 碰
  MING_KONG  = 'ming_kong',    // 明杠(杠别人的牌)
  AN_KONG    = 'an_kong',      // 暗杠(自己四张)
  BU_KONG    = 'bu_kong',      // 补杠(碰后再杠)
  HU         = 'hu',           // 胡
  ZI_MO      = 'zi_mo',        // 自摸
  PASS       = 'pass',         // 过
  DING_QUE   = 'ding_que',     // 定缺
}

/** 玩家游戏状态 */
interface PlayerGameState {
  playerId: string;
  seatIndex: number;           // 座位号 0-3
  handTiles: HandTiles;        // 手牌
  discardPile: Tile[];         // 弃牌堆
  dingQueSuit: TileSuit | null;// 定缺花色
  isHu: boolean;               // 是否已胡
  isTing: boolean;             // 是否听牌(下叫)
  huInfo: HuInfo | null;       // 胡牌信息
  score: number;               // 本局得分
}

/** 游戏房间状态 */
interface GameState {
  gameId: string;
  phase: GamePhase;
  players: PlayerGameState[];
  currentPlayerIndex: number;  // 当前操作玩家
  tileWall: TileWall;          // 剩余牌墙
  lastDiscard: {               // 最后出的牌
    tile: Tile;
    playerIndex: number;
  } | null;
  turnCount: number;           // 当前回合数
  roundNumber: number;         // 当前局数(第几局)
  totalRounds: number;         // 总局数(4或8)
  availableActions: ActionType[]; // 当前可执行的操作列表
}
```

### 4.3 番型定义

```typescript
// 番型类型枚举
enum FanType {
  // 基本番型
  PING_HU        = 'ping_hu',         // 平胡 (1番)
  DUI_DUI_HU     = 'dui_dui_hu',      // 对对胡 (2番)
  QING_YI_SE     = 'qing_yi_se',      // 清一色 (3番)
  QI_DUI         = 'qi_dui',          // 七对 (3番)
  LONG_QI_DUI    = 'long_qi_dui',     // 龙七对 (5番)
  QING_DUI       = 'qing_dui',        // 清对 (5番)
  QING_QI_DUI    = 'qing_qi_dui',     // 清七对 (6番)
  QING_LONG_QI   = 'qing_long_qi',    // 清龙七对 (8番)
  JIANG_DUI      = 'jiang_dui',       // 将对 (3番)
  JIANG_QI_DUI   = 'jiang_qi_dui',    // 将七对 (6番)
  
  // 带幺番型
  DAI_YAO        = 'dai_yao',         // 带幺九 (2番)
  QING_DAI_YAO   = 'qing_dai_yao',    // 清带幺 (4番)
  JIANG_DAI_YAO  = 'jiang_dai_yao',   // 将带幺 (4番)
  
  // 杠相关
  GEN            = 'gen',             // 根(四归一) (1番/根)
  
  // 特殊
  ZI_MO          = 'zi_mo',           // 自摸加番 (1番)
  GANG_SHANG_HUA = 'gang_shang_hua',  // 杠上花 (1番)
  GANG_SHANG_PAO = 'gang_shang_pao',  // 杠上炮 (1番)
  QIANG_GANG     = 'qiang_gang',      // 抢杠胡 (1番)
}

/** 胡牌信息 */
interface HuInfo {
  huType: 'zi_mo' | 'dian_pao';   // 自摸/点炮
  fromPlayerIndex?: number;         // 点炮玩家
  huTile: Tile;                     // 胡的牌
  fanTypes: FanType[];              // 番型列表
  totalFan: number;                 // 总番数
  isGangShangHu: boolean;           // 是否杠上花
  isGangShangPao: boolean;          // 是否杠上炮
  isQiangGang: boolean;             // 是否抢杠胡
}
```

### 4.4 房间与用户数据

```typescript
/** 房间配置 */
interface RoomConfig {
  totalRounds: number;     // 总局数 (4 / 8)
  baseScore: number;       // 底分
  maxFan: number;          // 封顶番数
  enableTimer: boolean;    // 是否启用出牌计时
  turnTimeout: number;     // 出牌超时(秒)
}

/** 房间信息 */
interface Room {
  roomId: string;
  roomNumber: string;      // 房间号(6位数字)
  config: RoomConfig;
  players: RoomPlayer[];   // 最多4人
  spectators: string[];    // 观战者ID列表
  gameState: GameState | null;
  status: 'waiting' | 'playing' | 'finished';
}

/** 房间内玩家 */
interface RoomPlayer {
  userId: string;
  nickname: string;
  avatar: string;
  seatIndex: number;
  isReady: boolean;
  isOnline: boolean;
  isAI: boolean;
  aiLevel?: number;        // AI等级 1-3
}

/** 用户账户 */
interface UserAccount {
  userId: string;
  phone: string;
  nickname: string;
  avatar: string;
  gold: number;           // 金币
  diamond: number;        // 钻石
  score: number;           // 积分(ELO)
  level: number;           // 段位等级
  winRate: number;         // 胜率
  totalGames: number;      // 总局数
  createTime: Date;
}

/** 对局记录 */
interface GameRecord {
  recordId: string;
  gameId: string;
  roomId: string;
  players: {
    userId: string;
    nickname: string;
    seatIndex: number;
    score: number;
    huCount: number;
    isWinner: boolean;
    fanDetails: { fanType: string; count: number }[];
  }[];
  totalRounds: number;
  startTime: Date;
  endTime: Date;
  isAIGame: boolean;
}
```

### 4.5 通信协议定义

```typescript
// shared/types/protocol.ts

/** WebSocket 消息类型 */
enum MessageType {
  // 房间相关
  ROOM_CREATE        = 'room:create',
  ROOM_JOIN          = 'room:join',
  ROOM_LEAVE         = 'room:leave',
  ROOM_READY         = 'room:ready',
  ROOM_START         = 'room:start',
  ROOM_INFO          = 'room:info',
  
  // 游戏操作
  GAME_DING_QUE      = 'game:ding_que',
  GAME_DISCARD       = 'game:discard',
  GAME_PONG          = 'game:pong',
  GAME_MING_KONG     = 'game:ming_kong',
  GAME_AN_KONG       = 'game:an_kong',
  GAME_BU_KONG       = 'game:bu_kong',
  GAME_HU            = 'game:hu',
  GAME_PASS          = 'game:pass',
  
  // 状态同步
  GAME_STATE_SYNC    = 'game:state_sync',    // 服务器→客户端 全量同步
  GAME_ACTION_RESULT = 'game:action_result', // 操作结果广播
  GAME_SETTLE        = 'game:settle',        // 结算广播
  
  // 匹配
  MATCH_START        = 'match:start',
  MATCH_CANCEL       = 'match:cancel',
  MATCH_FOUND        = 'match:found',
  
  // 系统
  HEARTBEAT          = 'sys:heartbeat',
  RECONNECT          = 'sys:reconnect',
  ERROR              = 'sys:error',
}

/** 通用消息结构 */
interface GameMessage<T = any> {
  type: MessageType;
  gameId?: string;
  roomId?: string;
  playerId?: string;
  data: T;
  timestamp: number;
}
```

---

## 五、游戏规则系统详细设计

### 5.1 基本规则

| 规则项 | 说明 |
|--------|------|
| 用牌 | 万、条、筒共108张(无风牌、箭牌、花牌) |
| 人数 | 4人 |
| 庄家 | 首局随机，后续胡牌者坐庄 |
| 摸牌 | 逆时针轮流摸牌 |
| 出牌 | 摸牌后必须出一张 |
| 吃牌 | 四川麻将**无吃牌** |
| 定缺 | 开局每人选择一门花色作为"缺"，胡牌时手牌不能含该花色 |

### 5.2 缺一门机制

```
开局后 → 每位玩家选择缺万/条/筒之一
       → 玩家必须在该花色全部打出后方可胡牌
       → 定缺花色通过"出牌"或"碰/杠"消耗
       → 系统自动检测是否仍含缺门花色
```

### 5.3 血战到底机制

```
玩家A胡牌 → A退出当前局，等待结算
         → 剩余3人继续，牌墙不动
玩家B胡牌 → B退出
         → 剩余2人继续
玩家C胡牌 → C退出
         → D流局，触发查大叫/查花猪
         → 或D自摸，结束
```

### 5.4 杠牌规则（刮风下雨）

| 杠类型 | 触发条件 | 收益规则 | 番数 |
|--------|----------|----------|------|
| **明杠(刮风)** | 手中有3张+别人打出第4张 | 被杠者付3倍底分 | 1番 |
| **暗杠(下雨)** | 手中有4张相同牌 | 其他三人各付2倍底分 | 1番 |
| **补杠(巴杠)** | 碰后摸到第4张 | 其他三人各付1倍底分 | 1番 |

**杠后特殊情况：**
- **杠上花**：杠牌后补摸的牌正好胡牌，额外加1番。
- **杠上炮**：杠牌后出的牌被别家胡，杠牌者付全部。
- **抢杠胡**：补杠时被别家胡，补杠者付全部。
- **呼叫转移（点炮转移）**：当玩家A杠牌（刮风下雨）获得点数，随后在摸起杠头牌打出（或直接用杠头牌）放炮被玩家B胡（即“杠上炮”），则玩家A在该次杠牌中所赢取的全部金币/分数必须转交给玩家B。
- **退税（杠钱退回）**：在对局结束流局查牌时，凡是判定为**“花猪”**（手牌仍有定缺花色）或**“未听牌”**（未下叫）的玩家，必须无条件退回本局对局中通过刮风下雨（明杠/暗杠/补杠）所赢取的全部金币/分数（退还给当时给钱的玩家）。已胡牌玩家不受此退税规则影响。

### 5.5 胡牌条件

```
基本胡牌牌型：4组顺子/刻子 + 1对将牌 (14张)
即：AAA + BBB + CCC + DDD + EE
```

**四川麻将特有规则：**
1. 必须缺一门（手牌不能含定缺花色）
2. 必须有番才能胡（不能无番胡/素胡）
3. 七对、龙七对等特殊牌型同样需满足缺一门

### 5.6 番型对照表

#### 核心番型

| 番型名称 | 番数 | 说明 |
|----------|------|------|
| 平胡 | 1番 | 基本胡牌牌型 |
| 对对胡 | 2番 | 4组刻子+1对将 |
| 清一色 | 3番 | 全部由一种花色组成 |
| 七对 | 3番 | 7个对子 |
| 龙七对 | 5番 | 七对中有1组为4张相同牌(根) |
| 将对 | 3番 | 全部由2/5/8组成 |
| 清对 | 5番 | 清一色+对对胡 |
| 清七对 | 6番 | 清一色+七对 |
| 清龙七对 | 8番 | 清一色+龙七对 |
| 将七对 | 6番 | 将对+七对 |

#### 带幺番型

| 番型 | 番数 | 说明 |
|------|------|------|
| 带幺九 | 2番 | 全部牌都含1或9 |
| 清带幺 | 4番 | 清一色+带幺九 |
| 将带幺 | 4番 | 将对+带幺九 |

#### 杠与特殊番

| 番型 | 番数 | 说明 |
|------|------|------|
| 根(四归一) | 1番/根 | 每组四张相同牌加1番 |
| 自摸 | 1番 | 自己摸到胡牌的牌 |
| 杠上花 | 1番 | 杠牌后补摸的牌胡牌 |
| 杠上炮 | 1番 | 杠牌后出的牌被胡 |
| 抢杠胡 | 1番 | 补杠时被抢胡 |

### 5.7 结算规则

#### 正常胡牌结算

```
点炮胡：点炮者支付 (底分 × 2^总番数)，其余2人不付
自摸胡：其余3人各支付 (底分 × 2^总番数)
```

#### 流局结算

```
查大叫(查叫)：未胡牌者需赔给已胡牌者
  - 已下叫(听牌)者：不用赔
  - 未下叫(未听牌)者：赔给已胡牌者 (底分 × 2^最大可能番数)

查花猪：流局时检查是否还有玩家手里有缺门花色的牌
  若存在：该玩家赔付其他所有玩家各 (底分 × 2^总番数)
```

#### 封顶规则

```
单局最多番数不超过房间设置的"封顶番数"（通常6或8番）
超过封顶按封顶计算
```

---

## 六、核心游戏流程设计

### 6.1 完整对局生命周期

```
[匹配/创建房间] → [4人准备就绪] → [游戏开始]
    → [第1局] → [洗牌发牌] → [定缺阶段]
    → [摸牌] → [出牌/碰/杠/胡判定循环]
    → [有人胡牌] → [血战继续/流局]
    → [第N局结束] → [终局结算]
```

### 6.2 单局详细流程

```
1. 洗牌
   - 生成108张牌数组，Fisher-Yates洗牌
   
2. 发牌
   - 庄家14张，其余3人各13张
   - 每人跳牌：庄家2+2+2+2+2+2+2，其余2+2+2+2+2+2+1(补一张)
   
3. 定缺
   - 4人同时选择缺门(万/条/筒)
   - 所有人选定后进入出牌阶段
   - 超时由系统自动选择(选手中数量最少的花色)
   
4. 出牌阶段（循环）
   当前玩家摸牌后：
   a. 系统检查是否可胡 → 有可胡牌则进入操作选择
   b. 系统检查是否可暗杠 → 提示
   c. 玩家选择出牌
   
   其他玩家对出的牌：
   a. 系统检查是否可胡(优先级最高)
   b. 系统检查是否可碰
   c. 系统检查是否可明杠
   d. 无人操作则下一家摸牌
   
5. 操作优先级：胡 > 杠 > 碰(无吃)
   同一优先级多人操作：按座位顺序(逆时针)先到先得
   
6. 杠后处理
   - 从牌墙末尾补摸1张
   - 检查是否杠上花
   - 或出牌(可能被杠上炮)
   
7. 胡牌判定流程
   a. 基本牌型判定(4面子+1雀头 或 七对)
   b. 缺一门判定
   c. 番型枚举计算
   d. 总番数 ≥ 1 方可胡
   
8. 流局判定
   - 牌墙剩余≤0(海底)且无人胡牌 → 流局
   - 执行查大叫、查花猪
```

### 6.3 状态机设计

```typescript
// server/src/engine/core.ts

class GameStateMachine {
  private state: GameState;
  
  // 状态转换表
  private transitions: Record<GamePhase, ActionType[]> = {
    [GamePhase.WAITING]:   [],
    [GamePhase.DEALING]:   [],
    [GamePhase.DING_QUE]:  [ActionType.DING_QUE],
    [GamePhase.PLAYING]:   [
      ActionType.DRAW, ActionType.DISCARD,
      ActionType.PONG, ActionType.MING_KONG,
      ActionType.AN_KONG, ActionType.BU_KONG,
      ActionType.HU, ActionType.PASS
    ],
    [GamePhase.SETTLING]:  [],
    [GamePhase.FINISHED]:  [],
  };
  
  // 核心方法
  deal(): void;              // 发牌
  dingQue(playerId, suit): void;  // 定缺
  draw(): Tile;              // 摸牌
  discard(playerId, tile): ActionType[];  // 出牌，返回其他玩家可选操作
  pong(playerId): void;      // 碰
  mingKong(playerId): void;  // 明杠
  anKong(playerId): void;    // 暗杠
  buKong(playerId): void;    // 补杠
  hu(playerId, type): HuInfo; // 胡牌判定
  checkWin(playerId, tile): HuInfo | null; // 检查是否可胡
  checkTing(playerId): boolean;  // 检查是否听牌
  settle(): SettleResult;    // 结算
}
```

### 6.4 胡牌算法

```typescript
// 核心胡牌判定算法（递归回溯）
function canWin(tiles: Tile[]): boolean {
  if (tiles.length === 0) return true;
  
  // 尝试作为雀头(将牌)
  if (tiles.length % 3 === 2) {
    if (tiles[0].code === tiles[1].code) {
      if (canWin(tiles.slice(2))) return true;
    }
  }
  
  // 尝试作为刻子(AAA)
  if (tiles.length >= 3 &&
      tiles[0].code === tiles[1].code &&
      tiles[1].code === tiles[2].code) {
    if (canWin(tiles.slice(3))) return true;
  }
  
  // 尝试作为顺子(ABC) - 仅限同花色连续
  const indices = findSequence(tiles); // 找同花顺子索引
  if (indices) {
    const remaining = tiles.filter((_, i) => !indices.includes(i));
    if (canWin(remaining)) return true;
  }
  
  return false;
}
```

---

## 七、房间与对局系统设计

### 7.1 房间生命周期

```
[创建房间] → [设置配置] → [等待加入]
    → [4人到齐/开始] → [对局中]
    → [对局结束] → [返回房间/解散]
```

### 7.2 匹配系统

```
[开始匹配] → [加入匹配队列(Redis Sorted Set)]
    → [按积分/段位筛选 ±N范围]
    → [凑齐4人] → [自动创建房间] → [开始对局]
    → [超时未凑齐] → [补AI玩家] → [开始对局]
```

### 7.3 观战系统

```
[进入观战] → [服务器推送当前GameState全量快照]
    → [后续增量推送每步操作结果]
    → [观战者不可操作] → [可随时退出]
```

### 7.4 断线重连

```
客户端检测断线 → 尝试重新连接
    → 重连成功：发送 RECONNECT 消息
    → 服务器返回完整 GameState 快照
    → 客户端恢复游戏状态
    
    → 重连超时(60s)：服务器托管(自动出牌)
    → 玩家回归后恢复手动操作
```

---

## 八、AI对手设计

### 8.1 AI架构

```
┌─────────────────────────────────────┐
│              AI 决策引擎               │
│  ┌─────────┐ ┌──────────┐ ┌───────┐ │
│  │ 手牌评估  │ │ 风险分析  │ │策略选择│ │
│  │ Evaluator│ │  Risk    │ │Strategy│ │
│  └─────────┘ └──────────┘ └───────┘ │
│  ┌─────────────────────────────────┐ │
│  │       决策树 (Decision Tree)      │ │
│  └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### 8.2 三级难度设计

| 难度 | 等级 | 特点 |
|------|------|------|
| 初级 | Lv.1 | 随机出缺门花色牌，不主动杠，不会防守 |
| 中级 | Lv.2 | 优先清缺门、评估搭子价值、会基本防守 |
| 高级 | Lv.3 | 听牌判断精准、主动杠牌、防守拆牌、推测对手 |

### 8.3 AI出牌策略

```typescript
// server/src/engine/ai-strategy.ts

class AIStrategy {
  level: number; // 1-3
  
  /** 选择要出的牌 */
  selectDiscard(hand: HandTiles, gameState: GameState): Tile {
    // 1. 优先出缺门花色的牌
    const queTiles = this.getQueMenTiles(hand);
    if (queTiles.length > 0) {
      return this.selectFrom(queTiles); // 根据难度选择策略
    }
    
    // 2. 评估每张牌的价值
    const scores = hand.concealed.map(tile => ({
      tile,
      score: this.evaluateTile(tile, hand, gameState)
    }));
    
    // 3. 根据AI等级添加随机扰动
    if (this.level === 1) {
      // 初级: 大随机
      return this.weightedRandom(scores);
    } else if (this.level === 2) {
      // 中级: 小随机
      return this.weightedRandom(scores, 0.3);
    } else {
      // 高级: 精确选择
      return scores.sort((a, b) => a.score - b.score)[0].tile;
    }
  }
  
  /** 评估牌的价值 */
  private evaluateTile(tile: Tile, hand: HandTiles, state: GameState): number {
    let score = 0;
    
    // 是否为孤张
    if (this.isIsolated(tile, hand)) score -= 10;
    // 是否破坏搭子
    if (this.breaksMeld(tile, hand)) score -= 20;
    // 是否为危险牌(可能放炮)
    if (this.isDangerous(tile, state)) score -= 30;
    // 是否靠近听牌
    if (this.nearsTing(tile, hand)) score += 15;
    
    return score;
  }
  
  /** 杠牌判断 */
  shouldKong(type: KongType, hand: HandTiles, state: GameState): boolean {
    if (this.level === 1) return Math.random() > 0.7; // 30%概率
    
    // 中级以上评估
    const risk = this.assessKongRisk(type, state);
    const benefit = this.assessKongBenefit(type, hand);
    return benefit > risk * (this.level === 2 ? 1.5 : 1.0);
  }
}
```

---

## 九、数据与账户系统设计

### 9.1 数据库表设计

#### 用户表 (users)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) PK | 用户ID(UUID) |
| phone | VARCHAR(11) UNIQUE | 手机号 |
| password_hash | VARCHAR(255) | 密码哈希 |
| nickname | VARCHAR(50) | 昵称 |
| avatar | VARCHAR(255) | 头像URL |
| gold | INT DEFAULT 1000 | 金币 |
| diamond | INT DEFAULT 0 | 钻石 |
| score | INT DEFAULT 1000 | ELO积分 |
| level | TINYINT DEFAULT 1 | 段位 |
| total_games | INT DEFAULT 0 | 总局数 |
| win_games | INT DEFAULT 0 | 胜局数 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### 对局记录表 (game_records)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) PK | 记录ID |
| game_id | VARCHAR(36) | 对局ID |
| room_id | VARCHAR(36) | 房间ID |
| total_rounds | TINYINT | 总局数 |
| is_ai_game | TINYINT(1) | 是否AI局 |
| start_time | DATETIME | 开始时间 |
| end_time | DATETIME | 结束时间 |

#### 玩家对局记录表 (player_game_records)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) PK | 记录ID |
| record_id | VARCHAR(36) FK | 对局记录ID |
| user_id | VARCHAR(36) FK | 用户ID |
| seat_index | TINYINT | 座位号 |
| final_score | INT | 最终得分 |
| hu_count | TINYINT | 胡牌次数 |
| is_winner | TINYINT(1) | 是否赢家 |
| fan_details | JSON | 番型明细 |

#### 房间表 (rooms)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) PK | 房间ID |
| room_number | VARCHAR(6) UNIQUE | 6位房间号 |
| config | JSON | 房间配置 |
| status | ENUM('waiting','playing','finished') | 状态 |
| created_by | VARCHAR(36) FK | 创建者 |
| created_at | DATETIME | 创建时间 |

#### 排行榜快照表 (rankings)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT AUTO_INCREMENT PK | ID |
| user_id | VARCHAR(36) FK | 用户ID |
| rank_type | ENUM('gold','score','winrate') | 排行类型 |
| rank | INT | 排名 |
| value | INT | 排行值 |
| snapshot_date | DATE | 快照日期 |

### 9.2 Redis 缓存设计

| Key | 类型 | 说明 | TTL |
|-----|------|------|------|
| `game:{gameId}` | Hash | 游戏状态缓存 | 对局结束 |
| `room:{roomId}` | Hash | 房间信息缓存 | 房间解散 |
| `match:queue` | Sorted Set | 匹配队列(score=ELO积分) | - |
| `user:{userId}:online` | String | 用户在线状态 | 5min |
| `user:{userId}:session` | Hash | 用户会话信息 | 24h |
| `lock:room:{roomId}` | String | 房间操作分布式锁 | 30s |
| `rank:gold:daily` | Sorted Set | 每日金币榜 | 24h |

### 9.3 金币/积分体系

| 行为 | 金币变化 | 积分变化 |
|------|----------|----------|
| 每日签到 | +100 | - |
| 完成一场对局 | +50 | - |
| 胡牌(自摸) | 按结算计算 | +15/局 |
| 胡牌(点炮) | 按结算计算 | +10/局 |
| 放炮 | 按结算计算 | -10/局 |
| 被自摸 | 按结算计算 | -5/局 |
| 观看广告 | +200 | - |
| 连胜奖励 | +连胜数×50 | +连胜数×5 |

---

## 十、UI/UX方案设计

### 10.1 设计原则

- **横屏优先**：麻将桌天然适合横屏布局
- **大按钮触控**：操作按钮不小于 44×44px
- **清晰信息层级**：手牌最大最清晰，其次操作区，最后辅助信息
- **风格定位**：经典国风+现代扁平化，深绿色牌桌底色

### 10.2 核心页面布局

#### 游戏桌面布局（横屏）

```
┌──────────────────────────────────────────────────────────┐
│  [房间号]              [局数 2/8]             [设置/退出]  │
├──────────────────────┬───────────────────────────────────┤
│                      │                                    │
│    对家手牌(背面)     │        对家信息 & 出牌区             │
│                      │                                    │
│  ┌────────────────┐  │  ┌─────────────────────────────┐  │
│  │ 左家 手牌(背面) │  │  │                             │  │
│  │                │  │  │       牌河 (弃牌区)           │  │
│  │                │  │  │   各家出的牌按方位排列         │  │
│  │  左家信息      │  │  │                             │  │
│  │  出牌区        │  │  └─────────────────────────────┘  │
│  └────────────────┘  │                                    │
│                      │    右家信息 & 出牌区                │
├──────────────────────┴───────────────────────────────────┤
│  [定缺: 万] 副露区: [碰]三筒 [杠]五条                      │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  🀇  🀈  🀉  🀊  🀋  🀌  🀍  🀎  🀏  🀙  🀚  🀛  🀜  │ │
│  │              ↑ 选中牌高亮上浮                           │
│  └─────────────────────────────────────────────────────┘ │
│              [碰]  [杠]  [胡]  [出牌]  [听牌提示]          │
└──────────────────────────────────────────────────────────┘
```

#### 大厅主页

```
┌───────────────────────────────────────────────┐
│  [头像] 昵称          金币:12888  钻石:50      │
├───────────────────────────────────────────────┤
│                                                │
│    ┌──────────┐  ┌──────────┐                 │
│    │          │  │          │                 │
│    │ 快速匹配  │  │ AI对战   │                 │
│    │          │  │          │                 │
│    └──────────┘  └──────────┘                 │
│    ┌──────────┐  ┌──────────┐                 │
│    │          │  │          │                 │
│    │ 创建房间  │  │ 加入房间  │                 │
│    │          │  │          │                 │
│    └──────────┘  └──────────┘                 │
│                                                │
├───────────────────────────────────────────────┤
│  [大厅]  [战绩]  [排行]  [商城]  [我的]         │
└───────────────────────────────────────────────┘
```

#### 结算页面

```
┌───────────────────────────────────────────────┐
│              本局结算 — 第2局                    │
├───────────────────────────────────────────────┤
│                                                │
│   🏆 赢家: 玩家A                                │
│   最终得分: +320                                │
│                                                │
│  ┌─────────────────────────────────────────┐  │
│  │ 排名  玩家    得分    胡牌  番型           │  │
│  │  1   玩家A   +320    2次  清一色3番       │  │
│  │  2   玩家B   +100    1次  对对胡2番       │  │
│  │  3   玩家C   -80     1次  平胡1番        │  │
│  │  4   玩家D   -340    0次  -              │  │
│  └─────────────────────────────────────────┘  │
│                                                │
│       [继续下一局]        [退出房间]             │
└───────────────────────────────────────────────┘
```

### 10.3 交互设计要点

| 交互点 | 设计方案 |
|--------|----------|
| 选择出牌 | 点击手牌上浮高亮，再次点击或按"出牌"按钮确认 |
| 碰/杠/胡 | 弹出操作面板，显示可选操作按钮(2秒倒计时) |
| 定缺选择 | 三色按钮(万/条/筒)，点击确认，倒计时15秒 |
| 摸牌动画 | 新牌从牌墙飞入手牌最右侧，带缓动动画 |
| 出牌动画 | 选中牌从手牌飞出至牌河对应位置 |
| 胡牌特效 | 全屏闪烁+金色"胡"字动画+番型逐条展示 |
| 杠牌特效 | "刮风/下雨"文字特效+番数飘字 |
| 音效 | 摸牌、出牌、碰、杠、胡各有独立音效 |

### 10.4 适配方案

```
横屏布局(主要): 16:9 / 18:9 / 19.5:9
竖屏布局(备选): 简化版牌桌，手牌缩小显示

分辨率适配:
- 基准设计: 1280 × 720 (横屏)
- Canvas 动态缩放，保持宽高比
- 使用 rem/vw 单位适配UI元素
- 安全区适配 (刘海屏/底部导航栏)
```

---

## 十一、技术实现要点

### 11.1 前后端类型共享

通过 `shared/` 目录实现前后端共用 TypeScript 类型定义，确保游戏状态、通信协议类型一致，避免接口不匹配问题。

### 11.2 服务端权威验证

所有游戏逻辑判定由服务端完成，客户端仅做预判和动画展示。客户端发送操作请求 → 服务端验证合法性 → 广播结果，防止作弊。

### 11.3 性能优化

| 优化点 | 方案 |
|--------|------|
| 手牌排序 | 客户端本地排序，O(n log n) |
| 胡牌检测 | 缓存中间结果，减少重复计算 |
| 番型枚举 | 一次性遍历所有番型，收集结果 |
| Canvas渲染 | requestAnimationFrame 驱动，脏矩形优化 |
| 网络消息 | Protobuf 或 MessagePack 压缩 |
| Redis缓存 | 活跃游戏状态存Redis，冷数据定时刷MySQL |

### 11.4 安全设计

| 层面 | 措施 |
|------|------|
| 传输安全 | HTTPS + WSS 加密通信 |
| 身份认证 | JWT Token + 刷新机制 |
| 操作防作弊 | 服务端全量验证，客户端仅做UI |
| 防刷金币 | 对局频率限制、异常检测 |
| 敏感操作 | 手机验证码二次确认 |

### 11.5 部署方案

```
┌──────────────────────────────────┐
│         Docker Compose            │
│  ┌────────┐ ┌────────┐ ┌──────┐ │
│  │ Nginx  │ │  App   │ │Redis │ │
│  │ :80    │ │ :3000  │ │:6379 │ │
│  └────────┘ └────────┘ └──────┘ │
│  ┌────────┐                      │
│  │ MySQL  │                      │
│  │ :3306  │                      │
│  └────────┘                      │
└──────────────────────────────────┘

移动端App: Capacitor 打包 Web 前端
Web端: 直接浏览器访问
```

---

## 十二、开发路线图

### 12.1 状态标记说明

| 标记 | 含义 | 新手理解 |
|------|------|----------|
| [x] | 已完成 | 已经能在当前项目里看到或运行 |
| [~] | 部分完成 | 有演示或雏形，但还不是完整功能 |
| [ ] | 未完成 | 方案里设计了，但当前项目还没有真正实现 |

### Phase 1 — MVP（核心对战，已完成）

- [x] 科幻麻将桌面 UI 原型
- [x] 麻将牌 SVG 视觉绘制
- [x] 头像、玩家状态、牌河、结算弹层等界面展示
- [x] 定缺选择交互：四家定缺、缺门优先出牌和缺门胡牌限制已接入
- [x] 出牌/摸牌演示：已使用真实牌墙，玩家出牌后东/北/西三家会自动轮流摸牌、出牌，再回到玩家摸牌
- [x] 麻将引擎：已完成牌库、洗牌、发牌、摸牌、出牌、胡牌、番型、查叫和查花猪
- [x] 基本游戏流程状态机：已完成轮转、碰/杠/胡、操作优先级、终局和血战继续
- [x] 完整缺一门 + 血战到底机制：已完成缺门限制、胡牌后退出和其余玩家继续
- [x] 碰/杠/胡合法性判断与真实操作：已完成判断、执行、补牌和结算记录
- [x] 单机4人本地对战（轮流出牌）：已完成三家电脑自动摸打和三档策略
- [x] 一局结束条件：已完成牌墙摸完、只剩一名未胡玩家和真实结算

### Phase 2 — 联网对战（代码与自动测试已完成，待真实联机验收）

- [x] WebSocket 实时通信
- [x] 房间创建/加入
- [ ] 匹配系统（当前版本采用房间号加入，未实现自动匹配）
- [x] 断线重连
- [x] 服务端权威验证

### Phase 3 — AI与结算（已完成）

- [x] AI对手(初/中/高级)
- [x] 完整番型计算
- [x] 刮风下雨结算
- [x] 查大叫/查花猪
- [x] 结算界面展示：已绑定真实番型与分数

### Phase 4 — 用户系统（代码与自动测试已完成）

- [x] 注册/登录
- [x] 金币/积分体系
- [x] 战绩统计
- [x] 排行榜
- [x] 数据库表和后端接口落地

### Phase 5 — 打磨上线（真机验收待完成）

- [~] UI/UX 视觉风格：已有科幻风原型，仍需按真实游戏流程优化
- [x] 动画：已和真实碰/杠/胡事件绑定
- [x] 音效
- [x] 移动端横屏适配
- [x] 手机安全区适配
- [x] 性能调优
- [x] Capacitor打包
- [ ] 真机测试与上线准备

### 12.2 推荐后续开发顺序

1. **已完成单机核心**：真实牌墙、发牌、摸牌、出牌、定缺和轮流操作均已接入。
2. **已完成规则与电脑玩家**：胡牌、番型、碰杠胡、查叫、查花猪和三档电脑策略均已接入。
3. **已完成联网与账号代码**：房间、重连、服务端校验、注册登录、金币和战绩均已有自动测试。
4. **当前唯一主线**：连接 Android 真机和在 Mac 上连接 iOS 真机，完成安装、横屏、触屏和完整对局验收。

生活化比喻：不要先修“网上约人进房间”，要先保证“这一桌麻将本地能按规则打完”。否则联网后只是把不完整的牌局同步给更多人看，问题会更难排查。

---

## 附录A：麻将牌编码对照表

| 编码 | 牌名 | 编码 | 牌名 | 编码 | 牌名 |
|------|------|------|------|------|------|
| W1 | 一万 | T1 | 一条 | G1 | 一筒 |
| W2 | 二万 | T2 | 二条 | G2 | 二筒 |
| W3 | 三万 | T3 | 三条 | G3 | 三筒 |
| W4 | 四万 | T4 | 四条 | G4 | 四筒 |
| W5 | 五万 | T5 | 五条 | G5 | 五筒 |
| W6 | 六万 | T6 | 六条 | G6 | 六筒 |
| W7 | 七万 | T7 | 七条 | G7 | 七筒 |
| W8 | 八万 | T8 | 八条 | G8 | 八筒 |
| W9 | 九万 | T9 | 九条 | G9 | 九筒 |

## 附录B：API接口清单

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/register | 用户注册 |
| POST | /api/auth/login | 用户登录 |
| GET | /api/user/profile | 获取个人信息 |
| PUT | /api/user/profile | 更新个人信息 |
| GET | /api/records | 获取对局记录 |
| GET | /api/records/:id | 获取单局详情 |
| GET | /api/rankings/:type | 获取排行榜 |
| GET | /api/stats | 获取战绩统计 |
| POST | /api/room/create | 创建房间 |
| POST | /api/room/join | 加入房间 |
| POST | /api/match/start | 开始匹配 |
| POST | /api/match/cancel | 取消匹配 |

---

> **文档版本**: V1.0 | **编制日期**: 2026-07-15 | **适用范围**: 指尖麻将-四川麻将血战到底 游戏开发
