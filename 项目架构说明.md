# 发财麻将项目架构说明

更新时间：2026-07-27

## 1. 架构目标

本项目继续使用当前的原生 JavaScript + Vite + Capacitor 技术栈，不更换框架，不改变现有页面、房间接口和游戏规则。

架构整理目标是把“页面启动”“麻将规则”“界面渲染”“联网通信”“本地设置”和“服务端能力”分开，方便新手定位问题，也方便后续把单机功能和联网功能继续扩展。

## 2. 当前分层

```text
项目根目录
├─ index.html                  页面骨架和可访问文本
├─ style.css                   桌面端全局样式
├─ mobile-landscape.css        手机横屏覆盖样式
├─ src/
│  ├─ app/
│  │  └─ main.js               前端应用入口和流程编排
│  ├─ mahjongCore.js           麻将领域规则：牌墙、胡牌、碰杠胡、结算、机器人
│  ├─ tileSvg.js               麻将牌视觉素材渲染
│  ├─ themeManager.js          主题切换
│  ├─ settingsManager.js       游戏设置和自动操作延迟
│  └─ errorLogger.js            前端异常收集和脱敏
├─ server/
│  ├─ index.js                 服务端启动入口
│  ├─ roomService.js            房间和牌局状态服务
│  ├─ webSocketServer.js        实时通信协议
│  ├─ accountService.js         账号、金币和战绩服务
│  └─ requestAuth.js            请求身份校验
├─ tests/                      自动化测试
├─ scripts/                    构建后检查脚本
└─ android/、ios/              Capacitor 原生工程
```

## 3. 依赖方向

```text
index.html
   ↓
src/app/main.js
   ├─→ src/mahjongCore.js
   ├─→ src/tileSvg.js
   ├─→ src/themeManager.js
   ├─→ src/settingsManager.js
   └─→ src/errorLogger.js

server/index.js
   ├─→ server/roomService.js ─→ src/mahjongCore.js
   ├─→ server/accountService.js
   ├─→ server/requestAuth.js
   └─→ server/webSocketServer.js
```

规则是唯一的“麻将裁判”：前端展示和服务端校验都调用 `src/mahjongCore.js`，不能在页面事件里重新写一套胡牌或碰杠胡判断。

## 4. 后续拆分边界

当继续拆分 `src/app/main.js` 时，按以下边界迁移，不一次性重写：

| 目标目录 | 负责内容 | 当前来源 |
|---|---|---|
| `src/api/` | 联网房间、WebSocket、请求封装 | `src/app/main.js` 中的房间通信函数 |
| `src/store/` | 当前牌局、联网房间、设置等状态 | `src/app/main.js` 中的全局状态变量 |
| `src/views/` | 牌桌、手牌、牌河、结算面板渲染 | `src/app/main.js` 中的 `render*` 函数 |
| `src/components/` | 定缺、操作按钮、设置面板等可复用交互 | `index.html` 与事件绑定函数 |
| `src/layout/` | 首页、牌桌工作区、移动端布局 | `index.html`、`style.css`、`mobile-landscape.css` |
| `src/utils/` | 本地存储、音效、时间和通用 DOM 辅助函数 | `src/app/main.js` 中的辅助函数 |
| `src/assets/` | 前端专用图片、图标和主题资源 | 当前 `public/` 与根目录素材 |

## 5. 本次整理范围

- 已将前端入口从根目录 `app.js` 移到 `src/app/main.js`。
- 已修正入口内所有相对导入路径。
- 已修正 `index.html` 的模块入口引用。
- 保留原有接口、页面结构、样式文件、测试命令和 Capacitor 工程。
- 暂不拆分业务函数，避免在已经通过 108 项自动测试的代码上进行大范围无依据重写。

## 6. 验收要求

- `node --test tests\\mahjongCore.test.js` 通过。
- `node --test tests\\*.test.js` 通过。
- `node .\\node_modules\\vite\\bin\\vite.js build` 通过。
- 浏览器可以正常加载首页和牌桌，控制台无错误。
- 桌面端和手机横屏都要检查首页、进入房间、定缺、出牌、设置和结算入口。
