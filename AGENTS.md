# 最高优先级：设备稳定与资源保护守则（严格禁止违反）

本守则高于本文件内的其他开发、测试、调试和效率要求。任何可能造成短时间 CPU/GPU 高负载、系统卡死、图形驱动崩溃或硬件保护重启的操作，必须以本守则为准。

## 严格禁止

- 严禁同时运行两个及以上高负载任务，包括：Android 模拟器、网页图形渲染、构建、自动化测试、开发服务器、批量文件处理。
- 严禁在模拟器运行期间启动构建、开发服务器、批量测试或其他图形密集型程序。
- 严禁连续、反复启动或重启模拟器来试错；一次调试只允许使用一台模拟器，并优先复用已启动实例。
- 严禁在 ADB 离线、模拟器异常、图形进程异常、CPU/GPU 占用未回落时继续发起启动、安装、截图或构建任务。
- 严禁为了恢复调试环境重启电脑；只允许停止由助手启动的 ADB、模拟器、构建或开发服务进程。
- 严禁为了“更快完成”绕过本守则中的限载、串行和熔断要求。

## 强制执行方式

1. 所有重任务必须串行执行：上一项完全结束并确认资源回落后，才能开始下一项。
2. 模拟器必须采用低负载配置：单实例、最多 2 个 CPU 核心、较低进程优先级、关闭启动动画和非必要图形效果；ADB 截图或安装完成后立即关闭。
3. 普通代码改动只运行必要的自动化测试和构建；只有确实需要手机画面验证时，才启动一次受限模拟器。
4. 出现 ADB 卡住、设备离线、模拟器异常、图形进程崩溃或资源持续偏高时，立即停止当前调试流程并报告原因；不得自动重试或循环重启。
5. 每次涉及模拟器、浏览器图形渲染、构建或批量测试前，先向用户说明即将执行的任务；如存在资源风险，先给出低负载方案再执行。
6. 测试证据优先使用已有截图、静态构建结果和单次 ADB 截图，禁止为截图反复启动图形环境。

---

# 项目开发规则

## 语言

- 所有沟通、说明、错误解释、测试结论都使用简体中文。
- 代码路径、命令、函数名、文件名保持原样，不翻译。

## 开发后必须自测

- 每次完成代码开发后，必须先运行可用的自动化验证命令。
- 当前项目优先使用：
  - `node --test tests\mahjongCore.test.js`
  - `node .\node_modules\vite\bin\vite.js build`
- 如果以后新增其他测试命令，也要一起执行。

## 必须诊断测试关键点

- 自测不能只说“通过了”，必须说明本次改动重点验证了什么。
- 关键点要按新手能看懂的方式写清楚，例如：
  - 哪条规则被验证了。
  - 哪个按钮或流程被验证了。
  - 哪些反例防止误判。
  - 是否检查了浏览器控制台错误。

## 必须截图留证

- 完成代码开发并自测后，必须在浏览器里打开或刷新本地页面进行检查。
- 对关键测试状态截图留证，截图统一保存到 `test-evidence/` 目录。
- 截图文件名要能看出用途和日期，例如：
  - `mahjong-self-test-evidence-2026-07-17.png`
  - `last-four-must-hu-evidence-2026-07-17.png`
- 最终回复用户时，要写明截图保存路径。

## GitHub 发布与双端安装包构建铁律（强制执行）

- **双端全量构建原则**：每次向 GitHub 发布提交代码（无论是推送到主分支、合并发布分支还是打发布 Tag）时，**一次提交必定同时带安卓（Android APK）和苹果（iOS IPA）两个安装包的自动化构建**，严禁单端发布，严禁遗漏任何一端。
- **发布前资源严格同步**：提交前必须执行前端生产构建并完成双端资源同步，确保 Android 和 iOS 工程内的 Web 资源与前端产物完全一致：
  - `npm run build`
  - `node ./node_modules/@capacitor/cli/bin/capacitor sync android`
  - `node ./node_modules/@capacitor/cli/bin/capacitor sync ios`
- **CI/CD 双端门控保障**：GitHub Actions 中的 `.github/workflows/build-android.yml`（Android APK 构建）与 `.github/workflows/build-ios.yml`（iOS IPA 构建）必须保持同频触发与可用。一次提交触发后，双端工作流必须全部构建成功并上传安装包产物；若任一端构建失败，该次发布视为不合格并立即阻断。

## 汇报要求

- 最终回复必须包含：
  - 改了哪些文件。
  - 跑了哪些测试命令。
  - 测试结果是否通过。
  - 关键测试点。
  - 截图证据路径。

