# 后端服务器地址（BUG-001 修复引入）
#
# 打包成手机 App 之后，网页是从手机本地加载的，程序没办法自己猜出后端在哪，
# 必须在构建时明确告诉它。不配置的话，App 里点“创建联机房间”会直接提示
# “服务器地址未配置”，而不是默默连到手机自己然后一直转圈。
#
# 用法：把本文件复制成 .env（或 .env.production），按实际情况改掉地址，再执行构建。
#
#   cp .env.example .env
#   node ./node_modules/vite/bin/vite.js build
#   npx cap sync android
#
# 内网联调：填开发机的局域网 IP（不是 localhost，手机上的 localhost 是手机自己）。
#   Windows 查 IP：ipconfig   → 找 IPv4 地址
VITE_SERVER_ORIGIN=http://192.168.1.10:3001

# 正式发布：必须用 https / wss，由 Nginx、Caddy 等反向代理终止 TLS，
# 再转发到内网的 http://127.0.0.1:3001。服务端本身不处理证书。
# VITE_SERVER_ORIGIN=https://mahjong.example.com

# ── 不用重新构建也能改地址的两种办法 ──
# 1) 部署期：直接编辑 dist/index.html，把 window.__MAHJONG_SERVER_ORIGIN__ 设成目标地址。
# 2) 运行期：在应用里打开设置面板填写服务器地址（存到 localStorage，优先级最高）。
